# tz_list

List available timezones

Domain: Extensions: time

---

### Description

`tz_list() : array`

Returns a list of available timezone names as strings. The list is derived
from the system zoneinfo database when available.

### Return Values

Returns an array of timezone names. The array may be empty if the zoneinfo
database is missing.
